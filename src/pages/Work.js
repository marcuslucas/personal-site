import Layout from "../components/layout/Layout";
import WorkCard from "../components/ui/WorkCard";

const Work = () => {
  return (
    <Layout>
      <WorkCard />
    </Layout>
  );
};

export default Work;
